const button = document.querySelector('.nav-links a');
    button.addEventListener('hover', ()=>{
       button.style.color ="brown"
    })
    button.addEventListener('mouseout',()=>{
       button.style.color ="black"
        
    })